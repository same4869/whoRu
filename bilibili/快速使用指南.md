# B站下载工具 - 快速使用指南

## 🎯 三步开始

### 1️⃣ 安装依赖
```bash
pip install -r requirements.txt
```

### 2️⃣ 下载视频/字幕
```bash
# 下载单个视频
python bili_downloader.py -v https://www.bilibili.com/video/BV1xx411c7mD

# 下载UP主视频（前10个）
python bili_downloader.py -u https://space.bilibili.com/123456 -n 10

# 只下载字幕
python bili_downloader.py -u https://space.bilibili.com/123456 -m subtitle_only -n 10
```

### 3️⃣ 转换字幕为文本
```bash
# 批量转换下载的字幕
python srt_to_txt.py -d ./downloads

# 或使用交互式工具
python 批量转换字幕.py
```

## 💡 常用命令速查

| 功能 | 命令 |
|------|------|
| 下载视频+字幕 | `python bili_downloader.py -v 视频URL` |
| 只下载字幕 | `python bili_downloader.py -v 视频URL -m subtitle_only` |
| 下载UP主前N个 | `python bili_downloader.py -u UP主URL -n 10` |
| 检查有无字幕 | `python check_subtitles.py -v 视频URL` |
| 转换字幕为文本 | `python srt_to_txt.py -d ./downloads` |

## ⚙️ 可选配置

### 配置Cookie（推荐）
```bash
# 1. 复制示例文件
cp bilibili_cookies.txt.example bilibili_cookies.txt

# 2. 用浏览器插件导出Cookie替换文件内容
# Chrome插件：Get cookies.txt LOCALLY
```

### 调整下载延迟（防风控）
编辑 `config.py`：
```python
DOWNLOAD_DELAY = 5  # 批量下载时延迟5秒
```

### 只下载中文字幕
编辑 `config.py`（已默认配置）：
```python
SUBTITLE_LANGS = ['ai-zh', 'zh-Hans', 'zh-CN', 'zh-Hant', 'zh-TW']
```

## 🔥 完整流程示例

```bash
# 1. 先检查UP主的视频是否有字幕
python check_subtitles.py -v https://www.bilibili.com/video/BV1xx411c7mD

# 2. 如果有字幕，批量下载（带延迟，防风控）
python bili_downloader.py -u https://space.bilibili.com/123456 -m subtitle_only -n 20 -o ./my_subs

# 3. 转换字幕为纯文本
python srt_to_txt.py -d ./my_subs

# 4. 查看生成的txt文件
```

## ❓ 常见问题

**Q: 下载没有文件？**
- 检查视频是否有字幕：`python check_subtitles.py -v URL`
- 很多视频只有弹幕，没有真正的字幕

**Q: 批量下载失败？**
- 增加延迟：修改 `config.py` 中的 `DOWNLOAD_DELAY`
- 分批下载：每次10-20个
- 配置Cookie

**Q: 字幕挤在一起？**
- 现在默认已保持分行，每句话独立一行

---

更多详细说明请查看 [完整文档](README.md)

